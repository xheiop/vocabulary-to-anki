# Only needed if you set [claude].provider = "api" in config.toml.
# The default "cli" backend uses your local `claude` login and needs no key.
# Copy to .env and fill in; loaded by the Makefile / launchd wrapper.
ANTHROPIC_API_KEY=sk-ant-...
